enum ServerMode { local, test, main }

enum ScaleHeight { veryLow, low, medium, high }

enum LoadStateStory { loading, success, notInit }

enum ButtonState {
  paused,
  playing,
  loading,
}

enum RepeatState {
  off,
  repeatSong,
  repeatPlaylist,
}
